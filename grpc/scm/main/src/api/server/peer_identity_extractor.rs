//! Peer identity extractor — type alias re-export; struct lives in api/server/types/.

/// Extractor for peer identity from DER-encoded leaf certificates.
pub type PeerIdentityExtractor = crate::api::server::types::PeerIdentityExtractor;
