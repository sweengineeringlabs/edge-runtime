# swe-edge runtime server — application configuration.
#
# RuntimeServer wires ingress + proxy + domain + egress, lifecycle management,
# and systemd notify into a single daemon entry point.
#
# Extends the shared template at C:\phd-systems\swelabs\edge\config\application.toml
# with server-specific defaults.

[runtime]
service_name           = "swe-edge"
http_bind              = "0.0.0.0:8080"
grpc_bind              = "0.0.0.0:50051"
shutdown_timeout_secs  = 30
systemd_notify         = false
grpc_allow_unauthenticated = false
grpc_reflection        = false

[observability.tracing]
enabled = true
format  = "pretty"   # pretty | json
level   = "info"     # trace | debug | info | warn | error
