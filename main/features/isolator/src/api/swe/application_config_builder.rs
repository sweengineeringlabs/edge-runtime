//! ApplicationConfigBuilder api interface marker.

/// Marker trait for the ApplicationConfigBuilder interface.
#[expect(dead_code, reason = "SEA api/ anchor — exported for consumers, not used internally")]
pub trait ApplicationConfigBuilder {}
