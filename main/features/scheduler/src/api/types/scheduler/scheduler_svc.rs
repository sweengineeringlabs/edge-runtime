//! [`SchedulerSvc`] — scheduler service entry point.
//!
//! All SAF factory methods are implemented as associated functions on this type.

/// Scheduler service entry point — call its associated functions to create
/// schedulers and config builders.
pub struct SchedulerSvc;
