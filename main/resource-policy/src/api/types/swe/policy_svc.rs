//! `PolicySvc` — public facade type for the resource policy subsystem.

/// SAF facade for the resource policy subsystem.
///
/// All construction entry points for resource policies and the primary
/// `SweEdgeRuntimeResourcePolicy` service are implemented as associated
/// functions on this type in `saf/`.
pub struct PolicySvc;
