//! Broker error contract.
//!
//! [`BrokerError`] is owned by the `swe-edge-message-broker` contract crate and
//! re-exported here so backends and the factory import it from a single theme path.

pub use swe_edge_message_broker::BrokerError;
