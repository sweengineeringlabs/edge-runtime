//! SAF — `ConfigValidator` public service surface.
pub use crate::api::ConfigValidator;
/// Identifies the `ConfigValidator` SAF contract in this crate.
pub const CONFIG_VALIDATOR_SVC: &str = "config_validator";
