# Changelog

## [Unreleased]

### Added
- Initial `HttpIngress` trait and value types (`HttpRequest`, `HttpResponse`, `HttpMethod`, etc.)
- `NoopHttpIngress` test stub
- `Validator` trait for request/config validation
